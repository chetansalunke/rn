class Category {
  constructor(category_id, name, features, image_url) {
    this.id = category_id;
    this.title = name;
    this.features = features;
    this.imageUrl = image_url;
  }
}

export default Category;